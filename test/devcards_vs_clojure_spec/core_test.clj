(ns devcards-vs-clojure-spec.core-test
  (:require [clojure.test :refer :all]
            [devcards-vs-clojure-spec.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
